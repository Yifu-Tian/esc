[project]
name = "mm-flow"
version = "0.1.0"
description = "MM-Flow generative replanning for mobile manipulators"
requires-python = ">=3.9"
dependencies = [
  "numpy",
  "scipy",
  "matplotlib",
]

[tool.setuptools.packages.find]
where = ["src"]

