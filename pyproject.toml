[project]
name = "storm"
version = "0.1.0"
description = "STORM structured trajectory generation for robot motion planning"
requires-python = ">=3.9"
dependencies = [
  "numpy",
]

[tool.setuptools.packages.find]
where = ["src"]
